// Routes to prerender. Add { path, snapshot } pairs here to prerender more pages.
// `snapshot` is the filename (without .html) inside frontend/prerendered/.
export const ROUTES = [
  { path: "/", snapshot: "landing" },
];
